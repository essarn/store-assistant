query Campaigns($category: String, $size: Int!, $page: Int!) {
  campaigns(category: $category, size: $size, page: $page) {
    items {
      code
      type
      name
      description
      image
      manufacturer
      volume
    }
    pagination {
      totalCount
      hasMore
    }
  }
}
