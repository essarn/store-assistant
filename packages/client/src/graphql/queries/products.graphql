query Products($query: String!, $size: Int!, $page: Int!) {
  products(query: $query, size: $size, page: $page) {
    items {
      code
      name
      image
      manufacturer
      volume
    }
    pagination {
      totalCount
      hasMore
    }
  }
}
